# SonarQube — Real Findings from Local Scan (authoritative issue list)

Generated by running a **local SonarQube 10.7 Community** server and scanning the repo with the
Phase-0 scoping (`src/app`, `project/ws`, `library/ws-widget`; vendored/build/e2e excluded).
Coverage fed from `coverage/lcov.info`. Full per-issue list (file:line:rule) is in
[`sonarqube-local-issues.csv`](./sonarqube-local-issues.csv).

> ⚠️ **Do NOT add `**/\*.html`to`sonar.exclusions`.** An earlier local setup did this to dodge a
SonarJS bridge failure, and it silently hid **268 templates and 228 real bugs**, producing a
false Reliability **A** locally while a clean server reported **C**. The bridge failure was not
caused by the HTML sensor at all (that sensor completes in ~12s); it was Windows `MAX_PATH`—
long paths are disabled, and the SonarJS bridge bundle extracts past 260 characters under the
default`.scannerwork`. The real fix is a short working directory:
`-Dsonar.working.directory=C:\sw`. The committed `sonar-project.properties` correctly does not
> exclude HTML; keep it that way so local, CI and SonarCloud agree.

## Measured ratings (project `cbp-local`)

| Axis            | Rating  | Count                    | vs SonarCloud original                                     |
| --------------- | ------- | ------------------------ | ---------------------------------------------------------- |
| Security        | **D**   | 6 vulnerabilities        | D (6) — matches                                            |
| Reliability     | **E**   | **310 bugs**             | E (770) — dropped (html + vendored/generated out of scope) |
| Maintainability | A       | 5,118 smells             | A — matches                                                |
| Security Review | **E**   | 97 hotspots, 0% reviewed | E (98) — matches                                           |
| Coverage        | 9.6%    | —                        | ~8.3% (lcov now wired)                                     |
| Duplications    | 15.8%   | —                        | 10.1%                                                      |
| Lines of code   | 137,986 | —                        | —                                                          |

The local scan faithfully reproduces the SonarCloud grades (Security 6 vulns and ~97 hotspots
match exactly), so it is a valid mirror for **targeting fixes and verifying them locally** without
needing SonarCloud access. Re-scan cadence: batch fixes by category, re-scan once per batch
(~16 min scan + ~7 min compute-engine on the embedded H2).

## Security — 6 Vulnerabilities (all `typescript:S2819`, CRITICAL)

`postMessage(..., '*')` sends data to any origin. **Sensitive** — the sub-application messages
carry the **Keycloak token + user profile**, so `'*'` is a real cross-origin leak.

| File                                                                         | Line         |
| ---------------------------------------------------------------------------- | ------------ |
| `library/ws-widget/utils/src/lib/services/auth-keycloak.service.ts`          | 219          |
| `library/ws-widget/utils/src/lib/services/subapplication-respond.service.ts` | 81, 122, 216 |
| `project/ws/viewer/src/lib/plugins/html/html.component.ts`                   | 590          |
| `project/ws/viewer/src/lib/plugins/iap/iap.component.ts`                     | 85           |

**Fix approach (needs care — do NOT blind-edit):** replace `'*'` with the actual content-frame
origin. The correct origin must come from the content/player host config, because a wrong origin
silently drops the message and breaks the content player (a load-bearing feature). Options, in
order of safety: (1) derive from a configured content-host origin if one exists; (2) restrict to
`window.location.origin` only where the frame is same-origin; (3) if the frame origin is genuinely
dynamic/unknowable, this is the rare case for a justified _won't-fix_ in SonarCloud. Each of the 6
must be assessed against how its target frame is created.

## Reliability — 310 Bugs by rule

**Safe / mechanical (recommended first — ~193):**

| Rule               | Count | What it is                                  | Fix                            |
| ------------------ | ----- | ------------------------------------------- | ------------------------------ |
| `css:S4649`        | 86    | `font`/`font-family` with no generic family | append `sans-serif` etc.       |
| `css:S4656`        | 42    | duplicate CSS properties                    | remove duplicate               |
| `css:S4657`        | 19    | invalid CSS property values                 | correct/remove                 |
| `css:S4653`        | 13    | unknown CSS properties                      | correct/remove                 |
| `css:S4670`        | 1     | `@import` after rules                       | reorder                        |
| `typescript:S3923` | 8     | all branches of if/switch identical         | collapse / fix intended branch |
| `typescript:S1862` | 5     | duplicate conditions in if-chain            | fix intended condition         |
| `typescript:S1656` | 5     | self-assignment (`x = x`)                   | remove / assign intended value |
| `typescript:S1764` | 4     | identical operands (`a === a`)              | fix intended operand           |
| `typescript:S905`  | 5     | statements with no effect                   | remove / complete              |
| `typescript:S4158` | 1     | always-empty collection use                 | fix                            |
| `typescript:S5842` | 1     | regex issue                                 | fix                            |

**Needs per-site review (~117):**

| Rule               | Count | What it is                                                                                                                                                                             |
| ------------------ | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `typescript:S2201` | 102   | return value of a side-effect-free call ignored (e.g. `.trim()`, `.map()`, `.includes()` result discarded) — often a real logic bug; fix by using the result or removing the dead call |
| `typescript:S4822` | 6     | promise errors not handled                                                                                                                                                             |
| `typescript:S6544` | 4     | floating/mishandled promise                                                                                                                                                            |
| `typescript:S4275` | 4     | getter/setter for the field mismatched                                                                                                                                                 |
| `typescript:S2871` | 4     | `.sort()` / compare without comparator                                                                                                                                                 |

Severity mix: **13 BLOCKER, 28 CRITICAL, 267 MAJOR, 2 MINOR.** Reliability reaches A only at **0
bugs**, so all must be fixed (or, for verified false positives, dispositioned in SonarCloud).

## Security Review — 97 Hotspots, 0% reviewed

Covered by [`sonarqube-hotspot-triage.md`](./sonarqube-hotspot-triage.md): overwhelmingly
**Safe** dispositions (DOM-id `Math.random`, trusted `bypassSecurityTrust`/`innerHTML`), applied by
review in the UI. ≥80% reviewed → Security Review A. (In this local server, review can also be done
via `POST api/hotspots/change_status`; on SonarCloud it's the Hotspots tab.)

## Local server (for reference)

- Dashboard: `http://localhost:9000/dashboard?id=cbp-local` (login `admin` / the password set at
  first boot). Portable install under `~/sq`, JDK + scanner in the session scratchpad.
- Re-scan command: `sonar-scanner` from repo root with
  `-Dproject.settings=<scratchpad>/sonar-local.properties -Dsonar.host.url=http://localhost:9000
-Dsonar.token=<token> -Dsonar.scm.disabled=true`.
- **Always pass `-Dsonar.working.directory=C:\sw`** (or any short path) on Windows. Without it the
  SonarJS bridge fails to start — "Failed to start the bridge server (300s timeout)" followed by a
  missing file inside `.scannerwork/.sonartmp/bridge-bundle/` — because long-path support is
  disabled and the bundle extracts past `MAX_PATH`. The missing file differs between runs, which
  makes it look like corruption rather than a path-length limit.
- **Do not exclude `**/\*.html`.\*\* See the warning at the top of this file: it hides the template
  rules entirely and yields a false A.
